{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "cartridge-runtime",
  "description": "Sandboxed cartridge/ST runtime windows. No dialog, fs, or shell plugin permissions.",
  "windows": ["cartridge_*"],
  "permissions": [
    "core:event:allow-listen"
  ]
}
